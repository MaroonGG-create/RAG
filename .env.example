# Server
SERVER_PORT=3000
CORS_ORIGIN=http://localhost:5173

# MySQL（容器与后端共用）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root123
DB_ROOT_PASSWORD=root123
DB_NAME=mini_rag

# Qdrant（本阶段后端不连接，T07 开始使用）
QDRANT_URL=http://localhost:6333

# Web
VITE_API_BASE_URL=/api

# 后续任务会按总体方案扩充其他环境变量。
