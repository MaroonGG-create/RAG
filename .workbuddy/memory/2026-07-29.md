# 2026-07-29 工作记录

## T05 文档文本解析任务设计

- 完成了 `docs/task-05-document-parsing.md` 的设计与编写（533 行，18 章节）
- 核心选型决策：pdfjs-dist@2.16.105（UMD 构建，Node 20 + CommonJS 兼容，原生逐页提取）
- 否决了 pdf-parse@1.1.1（单段拼接、无 pageNo）、pdf-parse@2.4.5（引入原生依赖）、pdfjs-dist@3.x/4.x（ESM 格式不兼容 CJS require）
- 暂存方案：文件系统 JSON（`{upload.dir}/.parsed/{documentId}.json`），不入库、零 migration
- 状态机：pending → parsing → (成功回 pending / 失败 failed + errorMessage)
- 幂等策略：暂存短路 + in-flight Promise 去重 + 原子 rename 覆盖写
- 手动触发：CLI 脚本 `pnpm --filter server parse:document <id>`，不新增 HTTP 接口

## T06 文本清洗与切片任务设计

- 完成了 `docs/task-06-text-cleaning-and-chunking.md` 的设计与编写（748 行，21 章节）
- 核心设计：接收 T05 的 ParsedDocument → 逐页 cleanText → 递归字符切分 → DocumentChunk 批量落库
- 清洗规则：换行归一化 + 删零宽字符 + 压缩连续空行 + trim；不清洗 Markdown 标记/标点/URL
- 切片算法：递归按 SEPARATORS=['\n\n','\n','。','！','？','. ','! ','? ',' ',''] 分割 + 合并重叠；不跨页
- 页码归属：切片不跨页边界，每 chunk 的 pageNo 唯一且准确
- 状态机：pending → chunking → (成功留 chunking / 失败 failed + chunkCount=0)
- 幂等：status=chunking AND chunkCount>0 短路；重跑时事务内 delete 旧 + insert 新
- 失败补偿：catch 中 delete chunks 兜底 + 置 failed + chunkCount=0
- 配置：新增 CHUNK_SIZE(默认500)/CHUNK_OVERLAP(默认100) 环境变量
- 文档详情切片预览：findOne 返回前 20 条 chunk（content 截 200 字）
- 手动触发：CLI 脚本 `pnpm --filter server chunk:document <id>`
- 与 T07 接口：T07 查 status=chunking AND chunk_count>0 拣选文档，读 document_chunk 表

## T07 Embedding 服务任务设计

- 完成了 `docs/task-07-embedding-service.md` 的设计与编写（1064 行，20 章节）
- 核心选型：Node.js 20 内置 fetch + AbortController 手写 HTTP 客户端，零新增依赖
- 否决 openai SDK（传递依赖冗余）、axios（Node 20 已有原生 fetch）
- 新建独立 EmbeddingModule（src/modules/embedding/），不侵入 processing 模块
- EmbeddingClient：纯 HTTP 客户端（仅依赖 ConfigService），含 Mock 分支 + 指数退避重试
- EmbeddingService：编排服务（读 DocumentChunk → 分批 → 状态流转 → in-flight 去重）
- 批处理：按 chunkIndex 升序分批串行调用 API，不并行（防限流）
- 重试策略：指数退避（base=1000ms, factor=2, max=30s, jitter）；429/5xx/超时重试，4xx 不重试
- 三重校验：向量数量/顺序/维度
- Mock 模式：EMBEDDING_MOCK=true，SHA-256 确定性伪向量 + L2 归一化
- 状态机：chunking → embedding → (成功留 embedding / 失败 failed)；completed 防御性拒绝
- 向量不持久化：embedDocument() 返回 EmbeddingResult 内存对象交给 T08
- 环境变量：EMBEDDING_BASE_URL/API_KEY/MODEL/DIMENSION/BATCH_SIZE/TIMEOUT_MS/MAX_RETRIES/MOCK
- CLI：pnpm --filter server embed:document <id>
- 与 T08 接口：T08 调 embedDocument() 获取向量 → 写 Qdrant → 置 completed

## T08 Qdrant 向量存储任务设计

- 完成了 `docs/task-08-qdrant-vector-store.md` 的设计与编写（1152 行，21 章节）
- 核心选型：`@qdrant/js-client-rest@1.12.0`（官方客户端，CJS 入口兼容 Node 20，与 Qdrant Server v1.12.4 对齐）
- 否决 fetch 手写（Qdrant REST API 复杂度高，需 ~200 行样板）
- 新建 VectorStoreModule（src/modules/vector-store/），imports EmbeddingModule
- QdrantClientWrapper：封装 @qdrant/js-client-rest + Mock 分支（内存 Map 模拟）
- VectorStoreService：OnModuleInit ensureCollection + storeDocument 编排 + deleteByFilter
- Collection 自举：不存在则创建（size=EMBEDDING_DIMENSION, distance=Cosine）+ 建索引（knowledgeBaseId/documentId）；已存在则校验维度/距离，不匹配 fail-fast
- T07→T08 边界：storeDocument 调 embeddingService.embedDocument() 获取向量 → deleteByDocumentId 清旧 → 批量 upsert → count 校验 → completed
- 状态机：embedding → completed（成功）/ failed（失败+清理半成品向量）；completed 防御性拒绝
- 幂等：每次先 deleteByDocumentId 再 upsert；upsert wait:true
- 失败补偿：catch 中 deleteByDocumentId 清理半成品 + 置 failed
- 文档/知识库删除集成：DocumentService.remove/KnowledgeBaseService.remove 注入 VectorStoreService，删除前清理向量（失败仅 warn 不阻止）
- Mock 模式：QDRANT_MOCK=true，内存 Map 模拟 Qdrant 全部操作
- 环境变量：QDRANT_COLLECTION(rag_chunks)/QDRANT_UPSERT_BATCH_SIZE(100)/QDRANT_MOCK(false)
- CLI：pnpm --filter server store:document <id>
- 与 T09 接口：completed 文档可被向量检索；T09 用 Qdrant search + filter knowledgeBaseId

## T09 向量检索任务设计

- 完成了 `docs/task-09-vector-retrieval.md` 的设计与编写（1204 行，24 章节）
- 核心设计：新建 retrieval 模块，编排 query embedding → Qdrant search → 结果映射
- 三层调用链：RetrievalService → EmbeddingService.embedQuery + VectorStoreService.search → QdrantClientWrapper.search
- Query Embedding 复用：EmbeddingService 新增 embedQuery(query) 方法，内部调 embeddingClient.embed([query])，不修改文档状态
- Qdrant search API：client.search(collection, {vector, filter, limit, score_threshold, with_payload:true})
- knowledgeBaseId filter：复用 T08 createKnowledgeBaseFilter() 模式
- TOP_K 默认 5（1-20），SCORE_THRESHOLD 默认 0.5（0-1），均支持请求级覆盖
- cosine 相似度：score 范围 [-1,1]，文本通常 [0,1]；Qdrant 默认降序返回
- 无命中返回空数组（非错误）；T10 据此判断是否调用 LLM
- payload 防御性校验：字段缺失/类型错误跳过 + warn 日志
- 已删除/无效文档过滤：查 document WHERE kb_id AND status=completed → Set<number>，内存过滤 Qdrant 结果
- T09 不修改文档状态（只读操作），不需要并发去重和重试
- Mock 检索：内存 cosine 相似度搜索；同文本 query+chunk → score=1.0（确定性验收）
- HTTP 接口：POST /api/knowledge-bases/:id/retrieve（T09 新增 #14）
- 内部 Service：RetrievalService.search() 供 T10 Chat 模块调用
- 异常映射：NotFoundException→404, EmbeddingFailure→502, VectorStoreFailure→500
- 环境变量：TOP_K(5)/SCORE_THRESHOLD(0.5)
- 与 T10 边界：T10 调 RetrievalService.search() → 获取 RetrievalResult[] → 组装上下文 → 调 LLM → 保存引用

## T10 RAG 生成任务设计

- 完成了 `docs/task-10-rag-generation.md` 的设计与编写（1616 行，24 章节）
- 核心设计：新建 llm 模块 + rag 模块，编排 检索 → 上下文组装 → Prompt 构建 → LLM 非流式调用 → answer+references
- LLM 客户端选型：Node.js 20 原生 fetch + AbortController 手写（同 T07 EmbeddingClient 模式），零新增依赖
- 否决 openai SDK（传递依赖冗余）、axios（功能等价于 fetch）
- 新建独立 LlmModule（src/modules/llm/），仅依赖 ConfigModule，exports LlmClient
- 新建 RagModule（src/modules/rag/），imports RetrievalModule + LlmModule，exports RagService
- RagService 复用 RetrievalService.search() 获取检索结果，不绕过不重写
- 无命中短路：results.length===0 → 返回固定话术，不调用 LLM（防编造第一道闸）
- Prompt 模板：System Prompt（中文规则+禁止编造+禁止标注编号+禁止内部元数据）+ User Prompt（[来源i] content + question）
- 防编造三道闸：①无命中不调 LLM ②System Prompt 只基于资料回答 ③不知道就说不知道
- 上下文截断：CONTEXT_MAX_CHARS 默认 4000，按 score 降序逐条拼接，超限停止；不硬截断单条 chunk
- references 与实际进入 Prompt 的 chunk 一一对应（usedResultCount）
- 不去重（chunkId 已唯一），按 score 降序（RetrievalService 保证）
- LlmClient：指数退避重试（base=1000 factor=2 max=30s jitter=500）；429/5xx/超时重试，4xx 不重试
- 异常映射：NotFoundException→404, EmbeddingFailure→502, LlmFailure→502, VectorStoreFailure→500
- Mock LLM：LLM_MOCK=true，确定性模板回答（基于来源数量），零网络调用
- 全链路 Mock：QDRANT_MOCK=true + EMBEDDING_MOCK=true + LLM_MOCK=true
- HTTP 接口：POST /api/knowledge-bases/:id/ask（T10 新增 #15）
- 内部 Service：RagService.ask() 供 T11 Chat 模块调用
- T10 不修改文档状态（只读操作）
- 环境变量：LLM_BASE_URL/API_KEY/MODEL/TEMPERATURE(0.3)/MAX_TOKENS(2048)/TIMEOUT_MS(60000)/MAX_RETRIES(3)/MOCK + CONTEXT_MAX_CHARS(4000)
- 与 T11 边界：T10 非流式 RagService.ask() + LlmClient.chat()；T11 新建 chat 模块做 SSE 流式 + Conversation + Message + MessageReference 落库

---

## T11 SSE 流式对话与会话管理 — 设计文档完成

- 文档路径：`docs/task-11-streaming-chat-and-conversation.md`（2210 行，31 节）
- 基线文档：00-overall-plan.md(v1.9) + 01-current-implementation.md(T10快照) + task-10-rag-generation.md + task-10-completion.md
- 覆盖用户提出的全部 15 个设计问题

### 核心设计决策
- API 路径：`POST /api/knowledge-bases/:id/chat`（与 T09/T10 路径风格一致，基线 v2.0 回填修订 #10）
- SSE 事件协议：metadata → token(多条) → references → done / error
- SSE 响应绕过包装：`@SkipResponseWrap()` + `ResponseInterceptor.isEventStream()` 双保险（已确认两者均已存在）
- 客户端断连：`req.on('close')` → `AbortController.abort()` → 取消 LLM fetch；已输出 token 仍落库（status=completed）
- 消息落库时机：用户消息先存（流式开始前）；助手消息在 token 全部收集后一次性 INSERT（不在流式中逐 token 写 DB）
- 事务边界：助手消息 + references 用 `DataSource.transaction()` 原子保存
- 会话标题：首条问题前 30 字符，无额外 LLM 调用
- 历史上下文：`CHAT_HISTORY_MAX_MESSAGES`(默认6) 取最近 completed 消息，拼入 LLM messages 数组
- 并发去重：`Map<number, AbortController>` 按 conversationId 去重，同会话新请求 abort 旧请求；在设 SSE headers 前检查，冲突返 409 普通 HTTP 响应
- 失败消息历史：`findRecentMessages` 过滤 `status='completed'`，不把失败助手消息当上下文
- Mock 流式：按 5 字符分块、每块延迟 50ms 输出，确定性可验收
- MessageReference 快照设计：documentId/chunkId 无 FK，文档删除后引用保留
- 级联删除：Conversation CASCADE → Message CASCADE → MessageReference（实体已配 @onDelete CASCADE）

### 模块结构
- 新建 `ConversationModule`：conversation.service.ts / message.service.ts / conversation.controller.ts / DTOs / module
- 新建 `ChatModule`：chat.service.ts / chat.controller.ts / chat.types.ts / sse-writer.ts / DTOs / module
- 修改 `LlmClient`：新增 `chatStream()` AsyncGenerator 方法（不改动现有 `chat()`）
- 修改 `llm.types.ts`：追加 `ChatStreamApiRequest`(stream:true) + `ChatStreamDelta`
- 修改 `configuration.ts` + `env.validation.ts`：追加 `chat.historyMaxMessages`
- 修改 `app.module.ts`：imports 追加 ConversationModule + ChatModule
- `.env.example`：追加 `CHAT_HISTORY_MAX_MESSAGES=6`

---

## T12 前端知识库与文档管理 — 设计文档完成

- 文档路径：`docs/task-12-frontend-knowledge-document.md`（1826 行，30 节）
- 基线文档：00-overall-plan.md(v2.0) + 01-current-implementation.md(T11快照) + task-11-streaming-chat-and-conversation.md + task-11-completion.md
- 覆盖用户提出的全部 10 个设计重点

### 核心设计决策
- 新增依赖：仅 `vue-router@^4.5`（不安装 Pinia，用 composables 替代）
- Axios 增强：新增 `ApiError` 类（extends Error，含 status + details），保留 HTTP 状态码用于差异化错误处理
- 路由：`/` → redirect `/knowledge-bases`；`/knowledge-bases` 列表；`/knowledge-bases/:id` 详情；`/knowledge-bases/:id/chat` T13 占位；`/health` 保留
- 文件上传：`UploadDragger` + `customRequest`（用项目 Axios 实例），`accept=".pdf,.md,.txt"`，`multiple=false`，`maxCount=1`，20MB 前端硬编码
- 上传错误处理：409 展示 details 中已存在文件信息，413/415/404 分别提示
- 文档状态：6 种状态映射（pending=default, parsing/chunking/embedding=processing, completed=success, failed=error + Tooltip errorMessage）
- 轮询：3 秒 setInterval，有非终态文档时启动，全部终态时停止，onUnmounted 清理；fetchList 显示 loading，refresh 静默
- 不编辑知识库：后端无 PUT/PATCH 接口，T12 禁止修改后端
- 不缓存数据：无 localStorage/sessionStorage，页面刷新重新从 API 查询
- T13 衔接：详情页"进入对话"按钮 → `/knowledge-bases/:id/chat` 占位页

### 前端工程结构
- 修改 4 个文件：package.json（+vue-router）、main.ts（+router）、App.vue（Layout+router-view）、http.ts（+ApiError）
- 新建 16 个文件：router/index.ts、types/{knowledge-base,document}.ts、api/{knowledge-base,document}.ts、composables/{use-knowledge-bases,use-documents}.ts、utils/format.ts、views/{KnowledgeBaseListView,KnowledgeBaseDetailView,ChatPlaceholderView}.vue、components/{KnowledgeBaseCard,CreateKnowledgeBaseModal,DocumentUploader,DocumentTable,DocumentStatusTag}.vue
- 当前 web 骨架：Vue3.5 + AntDV4.2 + Axios1.8 + Vite5.4 + TS5.6，仅 health 页面，无 router/Pinia

---

## T13 前端聊天页 — 设计文档完成

- 文档路径：`docs/task-13-frontend-chat.md`（约 1700 行，33 节）
- 基线文档：00-overall-plan.md(v2.1) + 01-current-implementation.md(T12快照) + task-11-streaming-chat-and-conversation.md + task-11-completion.md + task-12-frontend-knowledge-document.md + task-12-completion.md
- 覆盖用户提出的全部 12 个设计重点

### 核心设计决策
- SSE 客户端：`fetch` POST + `ReadableStream` + `TextDecoder`，不用 `EventSource`（仅支持 GET）
- 半包粘包处理：`buffer.split('\n\n')` 分帧，`frames.pop()` 保留最后不完整帧；多行 `data:` 拼接
- 客户端中止：`AbortController` + `abortSignal.aborted` 检查，中止时静默返回不调回调
- 状态机：`ChatGenerationStatus` = `idle → connecting → generating → completed/error/aborted`
- 防重复提交：`generationStatus` 状态门控，非 `idle` 状态禁止发送
- 流式增量更新：`ChatMessageItem` 流式态 `id:null, status:'streaming', createdAt:null`，token 事件 `content += delta`
- 引用绑定：SSE `references` 事件 → 设置 `message.references`；历史 API → `contentSnapshot` 字段；`getContent()` 用 `'contentSnapshot' in ref` 类型守卫统一处理
- conversationId 路由：URL query param `?conversationId=N`，`router.replace`（不产生新历史栈）
- 会话列表刷新：新会话 metadata 事件后 refresh，删除会话后 refresh，切换会话不刷新
- 引用折叠展开：默认折叠，`Collapse` 组件，单项可展开 content snapshot
- HTTP 错误 vs SSE 错误：HTTP 400/404/409 → `response.ok=false` → `ApiError`；SSE `error` 事件 → `onError` 回调
- 移动端响应式：`@media (max-width: 768px)` 会话列表用 `Drawer` 抽屉

### 文件清单
- 新建类型：`types/conversation.ts`、`types/chat.ts`
- 新建 API：`api/conversation.ts`、`api/sse.ts`、`api/chat.ts`
- 新建 composables：`composables/use-chat.ts`、`composables/use-conversations.ts`
- 新建组件：`ChatView.vue`、`ConversationList.vue`、`MessageList.vue`、`MessageBubble.vue`、`ReferencePanel.vue`、`ChatInput.vue`
- 修改：`router/index.ts`（替换占位路由为 ChatView）
- 删除：`views/ChatPlaceholderView.vue`
- 零新增依赖：fetch/ReadableStream/AbortController 均为浏览器原生 API

---

## T14 整体联调与交互收口 — 设计文档完成

- 文档路径：`docs/task-14-integration-and-polish.md`（约 900 行，23 节）
- 基线文档：00-overall-plan.md(v2.2) + 01-current-implementation.md(T13快照) + task-13-frontend-chat.md + task-13-completion.md
- 覆盖用户提出的全部 13 项范围和 10 步完整流程验收

### 核心发现（通过后端代码探索确认）
- 无 pipeline.service.ts：文档处理为 4 个独立 CLI 脚本手动触发（parse/chunk/embed/store）
- 知识库删除有磁盘文件泄漏：只清 Qdrant + DB，不清理 uploads/{kbId}/ 文件和 .parsed/ 缓存
- 6 处生产代码 console.error：http-exception.filter.ts(3) + main.ts(1) + health.service.ts(1) + database.service.ts(1)
- 无卡住文档恢复机制：中间状态(parsing/chunking/embedding)文档需手动 CLI 恢复
- README 严重过时：仍停留在 T04/T05 阶段，需全面重写
- .env.example 末尾有过时注释需删除
- 28 个环境变量经核对与 env.validation.ts 完全一致

### 核心设计决策
- 知识库删除增强：remove() 中查出所有文档 → 清理磁盘文件 + .parsed 缓存 + uploads/{kbId}/ 目录
- console.error → Logger.error：HttpExceptionFilter 用类级别静态方法（不在 DI 容器中）
- 新增 reset-stuck-documents CLI：重置卡在中间状态的文档为 pending
- README 全面重写：反映 MVP 完整功能，补充 CLI 命令、环境变量、Mock 模式说明
- T14 前端以验证和微调为主：T12/T13 已建立完善架构，不预期大规模改动
- 零新增依赖、不修改 DB 表结构、不修改 API 路径

### 文件清单
- 后端修改 5 个文件：knowledge-base.service.ts（删除磁盘清理）、http-exception.filter.ts、main.ts、health.service.ts、database.service.ts（console→Logger）
- 后端新增 1 个文件：scripts/reset-stuck-documents.ts
- 文档修改 3 个文件：README.md（重写）、.env.example（删注释）、00-overall-plan.md（追加 v2.3）
- 完成后生成：docs/reports/task-14-completion.md + 更新 docs/01-current-implementation.md
